const Post = require("../models/post");

// Like a post
const likePost = async (req, res) => {
    try {
        
    } catch (error) {
        res.status(500).json({ message: error.message });
    }
}

module.exports = {
    
}